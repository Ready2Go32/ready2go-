# Ready2Go 改善ステップ2

## 今回の変更

- 当日通知時刻を時計選択式に変更
- 前日通知時刻を時計選択式に変更
- 複数の通知時刻を追加・削除できる一覧を追加
- 通知設定がサーバーへ同期できなかった場合の案内を改善
- 予定編集画面に「この予定を削除」ボタンを追加
- キャッシュを `ready2go-v7` に更新

## 操作方法

通知時刻は時計から選択して「追加」を押し、最後に「通知設定を保存」を押します。
登録済み時刻の「×」を押すと、その時刻を一覧から外せます。

予定はカレンダー上の予定名をタップし、編集画面の「この予定を削除」を押します。
