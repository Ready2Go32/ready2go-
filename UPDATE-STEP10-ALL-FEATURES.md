# Step 10 — 全機能追加版

この版では、ホーム画面・通知・テスト・共有・自治体データ管理をまとめて追加しました。既存の予定と設定はPostgreSQL／端末保存に残るため、通常はファイルを上書きして再デプロイすれば引き継がれます。

## 追加したもの

- 初回設定を5段階で案内する進み具合表示
- 予定・天気・持ち物・ごみを集めた「明日の準備」
- 予定ごとの持ち物チェックリスト
- 雨・高温・低温に合わせた準備案内
- 予定30分前の天気・未完了の持ち物つきLINE通知
- LINE文章入力からの予定登録と、登録前の確認
- 通知履歴、失敗した通知の自動再試行、手動再送
- サーバー稼働状態とデータベース保存先の表示
- 自治体データの対応地区、有効期間、公式URL、最終確認日
- 招待コードで制限できる公開前テストモード
- 不具合／改善案の送信、スクリーンショット添付、投票
- 招待コードで参加する家族・グループ共有
- 設定項目の折りたたみ
- PWAショートカット（今日、明日、予定、ごみ）

## LINEから予定を登録する例

LINE公式アカウントへ、次の形式で送信します。

```text
8月30日 10時 歯医者
8月30日 10時30分 歯医者
毎週月曜日 16時 部活
```

Ready2Goが内容を返したら、`登録する`または`キャンセル`と送ります。毎週の予定は、最初の日から約1年間登録されます。

## テストモードを使う場合

RenderのWeb Serviceで「Environment」を開き、次を設定します。

| Key | Value |
|---|---|
| `TEST_MODE` | `true` |
| `TEST_INVITE_CODE` | グループだけに伝える任意のコード |

保存後に再デプロイします。テスト終了時は`TEST_MODE=false`に戻します。招待コードをGitHubへ書かないでください。

LINE Loginチャンネル自体が「開発中」の場合、LINE Developersで開発者権限を持つ人しかLIFFへログインできません。友達にも試してもらう段階では、LINE Loginチャンネルを公開し、Ready2Go側のテストモードで利用者を制限する構成にします。

## グループ予定

1. ホームの「グループ」を開く
2. 管理者がグループを作成する
3. 表示された招待コードをメンバーへ伝える
4. メンバーが「参加」にコードを入力する
5. 予定追加画面の「保存先」で共有先を選ぶ

「自分だけの予定」を選んだ予定は共有されません。共有予定は紫色の線とグループ名で区別されます。

## 通知について

- 予定の当日／前日通知、ごみ前日通知、予定30分前通知を送ります。
- 送信結果は「通知履歴」に残ります。
- 一時的な送信失敗は最大2回自動再試行します。
- 履歴の「再送する」で手動再送もできます。
- Render無料Web Serviceが停止している時間は、予定時刻どおりに処理を実行できない場合があります。通知を確実に運用する段階では常時稼働プランか、常時動くジョブ環境を使ってください。

## ホーム画面ウィジェットについて

この版はWebアプリ（PWA）なので、Android／iPhoneの本物のホーム画面ウィジェットはまだ追加していません。代わりに、アプリをホーム画面へ追加すると「今日」「明日」「予定」「ごみ」のショートカットが利用できます。本物のウィジェットは、将来ネイティブアプリ化するときの追加項目です。

## 上書き後の確認

1. Renderのデプロイが`Live`になるまで待つ
2. LINEからアプリを開き、初回設定ガイドを確認
3. 持ち物つき予定を明日に登録して「明日の準備」を確認
4. LINEテスト通知を送信し「通知履歴」を確認
5. テスト用グループを作り、別アカウントで招待コード参加を確認
6. ごみ情報の自治体データ画面で有効期限と公式URLを確認

以前の画面が残る場合は、LINE内ブラウザを閉じて開き直してください。Service Workerのキャッシュ名は`ready2go-v15`へ更新済みです。
