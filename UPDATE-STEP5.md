# Ready2Go 改善ステップ5

## ごみ収集機能の改善

- 都道府県・市区町村・町名／地区まで保存
- 公式情報を取得できない場合の手動収集ルール
- ごみの種類、曜日、毎週／第何週を設定
- 同じごみを複数曜日に登録
- 手動ルールの追加・削除を自動保存
- 手動設定もLINE前日通知に利用
- 次回収集日を画面上部に表示
- 祝日・年末年始・荒天時の変更警告
- 自治体公式URLと確認・更新日時を表示
- 既存の分別品目検索を継続
- 以前の地域保存形式との互換性
- 自治体データの画面表示を安全化
- キャッシュを `ready2go-v10` に更新

## 手動設定の使い方

1. 都道府県、市区町村、必要なら町名・地区を入力します。
2. ごみの種類、絵文字、曜日、毎週または第何週を選びます。
3. 自治体公式ページURLが分かれば入力します。
4. 「このルールを追加」を押すと端末とデータベースへ自動保存されます。

祝日などの変更は自治体によって異なるため、Ready2Goは警告と公式ページを表示します。最終確認は自治体公式情報で行ってください。
