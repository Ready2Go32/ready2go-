# Step 6：ごみ収集情報の自動設定

## 変更内容

- 都道府県・市区町村・町名を選ぶと自動検索します。
- 最初に共有データベースの確認済み自治体情報を検索します。
- 未登録の場合だけ自治体公式情報を検索します。
- 公式情報を確認できた結果は共有データベースへ保存し、次回から再利用します。
- 自動確認できない地域だけ手動設定欄を表示します。
- 「公式情報を再取得」ボタンも残しています。
- 公式URL・確認日時・祝日等の注意表示を維持します。

## 注意

自動検索にはRenderの `ANTHROPIC_API_KEY` が必要です。キーが未設定でも、すでに共有データベースへ登録された地域は自動表示できます。

町名・地区ごとに収集日が異なる自治体では、町名・地区も入力してください。公式情報を十分に確認できない場合は推測せず、手動設定へ切り替わります。

## 更新方法

ZIP内のファイルをGitHubへすべて上書きし、Renderが `Deploy live` になった後に確認してください。
